{
  "name": "ar-declination",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "testOld": "jest --config jestconfig.json",
    "test": "mocha --config .mocharc.json --watch tests/**/*.test.ts",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\" --no-error-on-unmatched-pattern",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rodzyk/ar-declination.git"
  },
  "keywords": [],
  "author": {
    "email": "rodzyk@programmer.net",
    "name": "Andriy Rodzyk",
    "url": "https://github.com/rodzyk"
  },
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/rodzyk/ar-declination/issues"
  },
  "homepage": "https://github.com/rodzyk/ar-declination#readme",
  "devDependencies": {
    "@types/mocha": "^9.1.1",
    "mocha": "^10.0.0",
    "prettier": "^2.7.1",
    "ts-node": "^10.9.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.7.4"
  },
  "files": [
    "dist/**/*"
  ]
}
